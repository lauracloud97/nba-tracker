import { ConferenceTypePipe } from './conference-type.pipe';

describe('ConferenceTypePipe', () => {
  it('create an instance', () => {
    const pipe = new ConferenceTypePipe();
    expect(pipe).toBeTruthy();
  });
});
